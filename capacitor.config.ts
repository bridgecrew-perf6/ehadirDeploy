import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'my.gov.pahang.smahadir',
  appName: 'smahadir',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
